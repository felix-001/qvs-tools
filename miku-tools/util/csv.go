package util

func LocadCsv(filename string) (rows [][]string) {
	/*
		file, err := os.Open(filename)
		if err != nil {
			log.Println(err)
			return nil
		}
		defer file.Close()


		reader := csv.NewReader(file)
		reader.FieldsPerRecord = -1 // Allow variable number of fields

		data, err := reader.ReadAll()
		if err != nil {
			log.Println(err)
			return nil
		}
		return data
	*/
	return
}
